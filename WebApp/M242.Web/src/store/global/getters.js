export const locale = (state) => {
    return state.locale
}

export const dateFormat = (state) => {
  return state.dateFormat
}

export const profileimagecachdate = (state) => {
  return state.profileimagecachdate
}